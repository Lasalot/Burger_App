import React from 'react';
import Aux from "../../hoc/Auxilliary";
import classes from "./Layout.module.css";

const layout = (props) => (
  <Aux>
  <div>Valami, 2,3</div>
    <main className={classes.Component}>Teszt</main>
    </Aux>
)

export default layout;